Bootstrap: docker
From: ubuntu:22.04

%post
    apt-get update && apt-get install -y \
        python3 python3-pip \
        && pip3 install numpy pandas matplotlib scikit-learn \
        && apt-get clean

%runscript
    exec "$@"