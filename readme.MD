created a container.def for container definition, do not commit the created container
create container with apptainer build container.sif container.def


Slurm file runner.slurm be run inside container